import 'package:flutter/material.dart';

/// FirstCry-inspired palette: soft blue + light orange accents.
class AppColors {
  const AppColors._();

  // Brand
  static const Color primarySoftBlue = Color(0xFF2F7DE1);
  static const Color primarySoftBlueLight = Color(0xFFE7F1FF);

  // Accents
  static const Color accentLightOrange = Color(0xFFFFA85C);
  static const Color accentLightOrangeSoft = Color(0xFFFFF1E6);

  // Surfaces
  static const Color surface = Colors.white;
  static const Color surfaceLightGray = Color(0xFFF6F7FA);

  // Text
  static const Color textPrimary = Color(0xFF1A1D21);
  static const Color textSecondary = Color(0xFF5B6472);

  // UI
  static const Color border = Color(0xFFE3E7EF);
  static const Color success = Color(0xFF20B26B);
  static const Color error = Color(0xFFE53935);
}

